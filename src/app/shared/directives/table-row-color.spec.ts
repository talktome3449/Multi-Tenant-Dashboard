import { TableRowColor } from './table-row-color';

describe('TableRowColor', () => {
  it('should create an instance', () => {
    const directive = new TableRowColor();
    expect(directive).toBeTruthy();
  });
});
